<Window x:Class="BarDbManagmentSystem.ErrorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Порушення обмежень СКБД" Height="500" Width="750" 
        WindowStartupLocation="CenterOwner" Background="#1A1A1A" ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="ErrorMessageText" Text="Сталася помилка цілісності даних" 
                   Foreground="#DC3545" FontSize="16" FontWeight="Bold" TextWrapping="Wrap" Grid.Row="0" Margin="0,0,0,10"/>

        <TextBlock x:Name="HintText" Text="Довідкова таблиця для перевірки доступних значень:" 
                   Foreground="LightGray" FontSize="13" Grid.Row="1" Margin="0,0,0,10"/>

        <DataGrid x:Name="ReferenceDataGrid" AutoGenerateColumns="True" Grid.Row="2" IsReadOnly="True"
                  Background="#252526" RowBackground="#2D2D30" AlternatingRowBackground="#333334" 
                  BorderThickness="1" BorderBrush="#3F3F46" HorizontalGridLinesBrush="#3F3F46" VerticalGridLinesBrush="#3F3F46"
                  AutoGeneratingColumn="ReferenceDataGrid_AutoGeneratingColumn">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#1F1F20"/>
                    <Setter Property="Foreground" Value="#00ADB5"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="8,5,8,5"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#DC3545"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="+ Створити новий запис" Width="200" Height="35" Margin="0,0,10,0"
            Background="#28A745" Foreground="White" FontWeight="Bold" BorderThickness="0" 
            Click="CreateNewReference_Click"/>

            <Button Content="Скасувати" Width="120" Height="35"
            Background="#6C757D" Foreground="White" FontWeight="Bold" BorderThickness="0" 
            Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>