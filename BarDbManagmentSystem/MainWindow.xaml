<Window x:Class="BarDbManagmentSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BarDbManagmentSystem"
        mc:Ignorable="d"
        Title="MainWindow" Height="1080" Width="1920">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ListBox x:Name="TableMenu" Grid.Column="0" Background="#121212" Foreground="White" 
                 SelectionChanged="TableMenu_SelectionChanged" Margin="10"/>

        <Grid Grid.Column="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="CurrenTableTitle" Text="Оберіть таблицю" 
               Foreground="Violet" FontSize="20" FontWeight="Bold" Margin="0,0,0,15" Grid.Row="0"/>

            <DataGrid x:Name="DynamicDataGrid" AutoGenerateColumns="True" Grid.Row="1" 
          Background="#252526" RowBackground="#2D2D30" AlternatingRowBackground="#333334" 
          BorderThickness="1" BorderBrush="#3F3F46" HorizontalGridLinesBrush="#3F3F46" 
          VerticalGridLinesBrush="#3F3F46" GridLinesVisibility="All"
          FontSize="14" FontFamily="Segoe UI" AutoGeneratingColumn="DynamicDataGrid_AutoGeneratingColumn">

                <DataGrid.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="Margin" Value="4,2,4,2"/>
                    </Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Foreground" Value="Black"/>
                        <Setter Property="Background" Value="White"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#1F1F20"/>
                        <Setter Property="Foreground" Value="#00ADB5"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10,6,10,6"/>
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                        <Setter Property="BorderBrush" Value="#3F3F46"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#00ADB5"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
            </DataGrid>
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="➕" Width="150" Height="35" Margin="5" Background="#28A745" Foreground="White" FontWeight="Bold" BorderThickness="0" Click="AddRowClick"/>
                <Button Content="❌" Width="150" Height="35" Margin="5" Background="#DC3545" Foreground="White" FontWeight="Bold" BorderThickness="0" Click="DeleteRowClick"/>
                <Button Content="💾" Width="170" Height="35" Margin="5" Background="#00ADB5" Foreground="White" FontWeight="Bold" BorderThickness="0" Click="SaveButtonClick"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
